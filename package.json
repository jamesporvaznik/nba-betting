{
    "name": "nba-betting",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run api\" \"npm run frontend\"",
        "api": "cd api && npm run dev",
        "frontend": "cd frontend && npm run dev"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
